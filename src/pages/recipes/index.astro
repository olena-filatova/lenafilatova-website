---
import BaseLayout from '../../layouts/BaseLayout.astro';
import RecipeHubBody from '../../components/RecipeHubBody.astro';
const B = import.meta.env.BASE_URL;
const title = 'Low-GI Recipes for Steady Blood Sugar — UK Measures';
const description = 'Free low-GI recipes for women 40+ — sugar-free bakes, low-carb mains and desserts, every one with a GI estimate. UK metric measures, English and Ukrainian.';
---
<BaseLayout
  lang="en"
  title={title}
  description={description}
  canonical="/recipes/" alternates={{ en: "/recipes/", ua: "/ua/recipes/" }}
  showList={true}
  enHref={`${B}recipes/`}
  uaHref={`${B}ua/recipes/`}>
  <RecipeHubBody lang="en" />
</BaseLayout>
